# Generate a new SECRET_KEY with: python -c "import secrets; print(secrets.token_urlsafe(64))"
SECRET_KEY=CHANGE_ME_TO_A_RANDOM_SECRET_KEY
DEBUG=False
ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com

DB_NAME=pos_system
DB_USER=your_db_user
DB_PASSWORD=your_secure_password
DB_HOST=localhost
DB_PORT=5432

# CORS - comma separated origins (your frontend URL)
CORS_ALLOWED_ORIGINS=https://yourdomain.com

# Cookie settings
COOKIE_DOMAIN=yourdomain.com
COOKIE_SECURE=True

# Fiscal certificates
# CERT_P12_PASSWORD=your_certificate_password
